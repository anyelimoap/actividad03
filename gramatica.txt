
IF → if (COND) { INSTRUCCIONES } else { INSTRUCCIONES }
CONT → expr relop expr
relop → < | > | == | !=


DECL → tipo ID = valor ;
tipo → int | float | char

ID → letra (letra | dígito)*
INSTR → ID = art 
art → (E | ID) op (E | ID) 
op → + | - | * | /
