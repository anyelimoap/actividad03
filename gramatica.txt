Grámatica base para el desarrollo de la actividad



IF → if (COND) { INSTRUCCIONES } else { INSTRUCCIONES }
COND → expr relop expr
relop → < | > | == | !=


DECL → tipo ID = valor ;
tipo → int | float | char

ID → letra (letra | dígito)*


DECL → tipo ID 
DECL → tipo ID = valor ;
tipo → int | float | char
